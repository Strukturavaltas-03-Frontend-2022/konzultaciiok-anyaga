import { PipeRunnerPipe } from './pipe-runner.pipe';

describe('PipeRunnerPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeRunnerPipe();
    expect(pipe).toBeTruthy();
  });
});
